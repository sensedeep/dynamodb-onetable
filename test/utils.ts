/*
    utils.ts - Extercise test/utils/init
 */
import {print, dump, delay} from './utils/init'

// jest.setTimeout(7200 * 1000)

test('Exercise utils/init for coverage', async () => {
    /*
    dump({when: new Date()})
    print()
    await delay(0)
    */
})
